{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py runserver && python -m pip install -r requirements.txt && python manage.py collectstatic --noinput --settings=EPLInsights.settings && python manage.py migrate --settings=EPLInsights.settings && gunicorn --workers=4 EPLInsights.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
    }
  }